name: taccan_mobile
description: Taccan — multiplayer Codenames mobile client
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  socket_io_client: ^3.0.2
  shared_preferences: ^2.5.3
  go_router: ^15.1.2
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
