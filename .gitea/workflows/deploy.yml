name: Deploy

on:
  push:
    branches: [main]

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: node --test test/*.test.js

      - name: Deploy
        run: |
          apt-get update -qq && apt-get install -y -qq rsync > /dev/null
          rsync -a --delete --exclude node_modules --exclude .git ./ /opt/sancak/taccan/
          docker compose -f /opt/sancak/docker-compose.yml up -d --build taccan
